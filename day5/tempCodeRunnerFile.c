 if (a[lo] == key) {
       printf("Founf at %d\n",lo);
             
    }
    else if (a[hi] == key) {
        printf(" Found at %d",hi);
       
    }
    else {
        printf(" Not found");
    }