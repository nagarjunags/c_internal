/* programs to reverse words in the given string
        INPUT:
            jack and jill
        OUTPUT:
            kcaj dan llij*/



#include<stdio.h>
#include<stdlib.h>

int rev_word(char *q)
{
    int z= *q==' '?0:*q=='\0'?0:rev_word
}

int main(void)
{
    int i=0;
    char *p=(char *)malloc(100*sizeof(char));
    printf("Enter the string:");
    scanf("%[^/n]s",p);
    char *q=(char *)malloc(100*sizeof(char));
    for(;p[i]!=' ';i++)
    {
        
    }

    return 0;
}